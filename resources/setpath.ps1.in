# ***************************************************************
# * This script adds kali to the current path on Windows.
# ***************************************************************

$env:KALI_DIR = Get-Location
$env:PATH = $env:KALI_DIR + ";" + $env:PATH
$env:PYTHONPATH = $env:KALI_DIR + "\python" + ";" + $env:PYTHONPATH
